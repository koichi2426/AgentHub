# =================================
# AgentHub 環境設定ファイル (.env)
# =================================

# ---------------------------------
# データベース接続情報 (MySQL)
# ---------------------------------
# これらの値は開発用です。本番環境ではより強力なパスワードに変更してください。

DB_USER=
DB_PASSWORD=
DB_ROOT_PASSWORD=
DB_NAME=
DB_HOST=
DB_PORT=

# FastAPIバックエンドが使用するデータベース接続URL
# 変数名をAlembicと統一

DATABASE_URL=

# ---------------------------------
# JWT 設定
# ---------------------------------
SECRET_KEY=
ALGORITHM=
ACCESS_TOKEN_EXPIRE_MINUTES=

# ---------------------------------
# Frontend (Next.js) 用
# ---------------------------------
# ブラウザからアクセスするAPIのURL

NEXT_PUBLIC_API_URL=
# Next.jsサーバーからアクセスするAPIのURL (コンテナ間通信)

API_URL_INTERNAL=

# ---------------------------------
# Celery / Redis 設定
# ---------------------------------
CELERY_BROKER_URL=
CELERY_RESULT_BACKEND=
CELERY_TASK_TIME_LIMIT=
CELERY_TASK_MAX_RETRIES=

# ---------------------------------
# SFTP Storage Settings
# ---------------------------------
# (Docker Secretsを使用せず、こちらで一元管理します)

# --- VPSサーバー情報 ---

VPS_IP=
VPS_USER=
# パスワード認証を無効化したためコメントアウトしています

# VPS_ACCOUNT_PASSWORD=

# --- SSH秘密鍵設定 ---
# Dockerホスト側の秘密鍵ファイルのパス（例: /home/satoy/.ssh/id_ed25519）

VPS_KEY_HOST_PATH=
# コンテナ内でマウントされるパス（例: /root/.ssh/id_ed25519）

VPS_KEY_FILE_PATH=

# --- SSHポート番号 ---

VPS_PORT=

# --- 転送ディレクトリ設定（Linux 実体パスを指定） ---
# ※ Windows (/mnt/c/...) ではなく、Linux実体の /home/satoy/... を使用

VPS_TRAINING_DIR=
VPS_MODEL_DIR=
VPS_VISUALS_DIR=

# ---------------------------------
# C++ Engine Settings (Nginx)
# ---------------------------------
AGENTHUB_ENGINE_BASE_URL=